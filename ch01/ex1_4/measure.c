#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <time.h>

int main(void) {
    /* TODO: malloc a 256-byte buffer */
    /* TODO: snprintf "sqrt(2) = %.10f" into the buffer using sqrt(2.0) */
    /* TODO: printf the buffer */
    /* TODO: printf "time = %ld\n" using (long)time(NULL) */
    /* TODO: free the buffer */
    return 0;
}
